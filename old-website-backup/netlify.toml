[build]
  # This is a static site, no build command needed
  # Netlify will just serve the files as-is
  publish = "."
  
# Explicitly disable any Python/runtime detection
[build.environment]
  NODE_VERSION = ""
  PYTHON_VERSION = ""
  RUBY_VERSION = ""

